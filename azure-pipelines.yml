# Starter pipeline

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |
    echo Add other tasks to build, test, and deploy your project.
    echo See https://aka.ms/yaml
  displayName: 'Run a multi-line script'
- task: AzureCLI@2
  inputs:
    azureSubscription: 'enmaxtraining_sc'
    scriptType: 'ps'
    scriptLocation: 'inlineScript'
    inlineScript: 'az storage account create –name gitsstorage –resource-group enmaxtraning –location WestEurope –sku Standard_LRS –kind StorageV2'